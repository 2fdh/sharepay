{% extends "layout.njk" %}

{% block content %}

  Activity details  :

  <li>{{ activity.title }}</li>
  <li>{{ activity.description }}</li>

  {% for att in attendee %}
  <li>{{ att.name }}</li>

  {% endfor %}

<form method="POST" action="/activities/{{ activity.id }}">
  <button type="submit">
    Close Activity!
  </button>
</form>

{% endblock %}
